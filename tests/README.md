# Tests

iOS unit tests are in `IOSFinalProject_FTests/` and UI tests in `IOSFinalProject_FUITests/`.

Run tests in Xcode with `Cmd+U` or from the command line:

```bash
xcodebuild test \
  -scheme IOSFinalProject_F \
  -destination 'platform=iOS Simulator,name=iPhone 15'
```
